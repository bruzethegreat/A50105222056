{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "A reusable logging middleware package for API logging",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["logging", "middleware", "api", "typescript"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^24.3.1",
    "axios": "^1.11.0",
    "typescript": "^5.9.2"
  }
}
